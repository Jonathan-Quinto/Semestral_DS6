<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskFlow">

        <!-- LAUNCHER apunta a SplashActivity para el flujo correcto de autenticación -->
        <activity android:name=".ui.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.LoginActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.RegistroActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.MainActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.AgregarTareaActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.EditarTareaActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.NuevaTareaActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.CategoriasActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.EstadisticasActivity"
            android:exported="false" />

        <activity android:name=".ui.activities.PerfilActivity"
            android:exported="false" />

    </application>
</manifest>
